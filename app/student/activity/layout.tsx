export const metadata = {
    title: "student - vconnect",
    description: "Access your account",
  };
  
  const SignupLayout = ({ children }) => {
    return (
      <html lang="en">
        <body>
          <main>{children}</main>
        </body>
      </html>
    );
  };
  
  export default SignupLayout;
  